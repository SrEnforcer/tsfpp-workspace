---
mode: agent
description: "Invoke the Trunk Bot agent for commit, merge, push, and ship operations"
---

Activate the Trunk Bot agent: [git-trunk-bot.agent.md](../agents/git-trunk-bot.agent.md).

Use for: `commit`, `merge-to-main`, `push`, and `ship` operations via `scripts/trunk.sh`.

For full feature lifecycle orchestration (start-work → checkpoint → sync → open-pr), use [git-feature-lifecycle.prompt.md](./git-feature-lifecycle.prompt.md) instead.
