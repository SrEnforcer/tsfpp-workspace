allowBuilds:
  esbuild: true
# Exclude our own internal packages from the minimum-release-age supply-chain
# check. Package-name-only (no @version) is intentional: it covers every
# resolved version, so these entries never need editing on a version bump.
minimumReleaseAgeExclude:
  - '@tsfpp/eslint-config'
