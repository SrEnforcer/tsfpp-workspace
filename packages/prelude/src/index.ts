/**
 * @module index
 *
 * Public surface of the `prelude` package.
 * Re-exports all functional primitives from fp.ts.
 *
 * Import Ramda combinators via the `prelude/ramda` subpath (TSF++ Rule 13.1).
 */
export * from './fp.js';
